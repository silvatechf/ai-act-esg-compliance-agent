# ai_agent/requirements.txt

# Framework principal para expor a lógica Python como API REST
fastapi

# Servidor ASGI para rodar o FastAPI (precisa ser instalado separadamente do fastapi)
uvicorn

# Biblioteca usada para validar a estrutura de dados (usada em data_models.py)
pydantic